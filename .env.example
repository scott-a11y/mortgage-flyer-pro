# === Mortgage Flyer Pro — Environment Variables ===

# --- Client-side (bundled into the app via Vite) ---
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- Server-side (Vercel only — NEVER bundled into the client) ---
GEMINI_API_KEY=
BRIDGE_BEARER_TOKEN=
BRIDGE_DATASET_ID=
RMLS_BEARER_TOKEN=
